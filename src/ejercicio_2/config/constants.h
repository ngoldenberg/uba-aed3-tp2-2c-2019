#pragma once

#include <map>
#include <string>
#include <memory>
#include <vector>
#include <list>

using namespace std;

typedef vector<vector<float>> FloatMatrix;

typedef vector<list<pair < int, float>>>
AdjList;
